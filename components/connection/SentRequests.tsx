import { StyleSheet, Text, View } from "react-native";
import React from "react";

const SentRequests = () => {
  return (
    <View>
      <Text>SentRequests</Text>
    </View>
  );
};

export default SentRequests;

const styles = StyleSheet.create({});
