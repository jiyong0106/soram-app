import { StyleSheet, Text, View } from "react-native";
import React from "react";

const ReceivedRequests = () => {
  return (
    <View>
      <Text>ReceivedRequests</Text>
    </View>
  );
};

export default ReceivedRequests;

const styles = StyleSheet.create({});
