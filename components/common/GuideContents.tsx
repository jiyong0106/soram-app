import { StyleSheet, Text, View } from "react-native";
import AppText from "./AppText";

const GuideContents = () => {
  return (
    <View>
      <AppText>출입증 이용을 위해 앱 업데이트가 필요합니다</AppText>
    </View>
  );
};

export default GuideContents;

const styles = StyleSheet.create({});
