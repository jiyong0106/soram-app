import Button from "../common/Button";

const LoginButton = () => {
  return (
    <Button
      label="로그인"
      color="#fff"
      textColor="#222"
      borderColor="#FF835C"
    />
  );
};

export default LoginButton;
